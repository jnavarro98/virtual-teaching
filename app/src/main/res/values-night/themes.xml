<resources>
    <!-- Base application theme. -->
    <style name="Theme.VirtualTeaching" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/background_color</item>
        <item name="colorPrimaryVariant">@color/background_color</item>
        <item name="colorOnPrimary">@color/foreground_color</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/foreground_color</item>
        <item name="colorSecondaryVariant">@color/foreground_color</item>
        <item name="colorOnSecondary">@color/background_color</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>